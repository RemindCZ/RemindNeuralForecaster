# Assets

The canonical 0.8.2 visual assets are the real application captures under
`../screenshots/`.

The existing `manual/` images and standalone HTML brochures were present before
this audited package. Preserve them as historical source material, but verify
their text and UI against 0.8.2 before reuse. They are not referenced by the new
website, feature, changelog, GitHub or social copy.

